fastapi==0.115.0
starlette==0.38.6
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
# psycopg2-binary==2.9.9  # PostgreSQL driver — only needed if DATABASE_URL is postgresql://
#   No wheel exists for Python 3.13, so pip compiles from source and fails without libpq.
#   Uncomment (and `brew install postgresql`) only when switching off SQLite.
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
redis==5.0.8
email-validator==2.2.0
fastapi-mail==1.4.1
