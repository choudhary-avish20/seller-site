ENV=development
DEBUG=true

# ── Database ──────────────────────────────────────────────────────────────────
# Local dev: SQLite, works out of the box with no extra services.
DATABASE_URL=sqlite:///./wholesale.db

# Production (Neon): copy the connection string from your Neon project dashboard.
# Project → Connection Details → select "psycopg2" driver → copy the URL.
# It looks like this (replace the placeholders):
# DATABASE_URL=postgresql://user:password@ep-xxx-yyy.us-east-2.aws.neon.tech/dbname?sslmode=require

# ── Auth ──────────────────────────────────────────────────────────────────────
SECRET_KEY=change-me-in-prod
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ── CORS ──────────────────────────────────────────────────────────────────────
# Production: set to your actual frontend URL, e.g. ["https://www.yourdomain.com"]
CORS_ORIGINS=["http://localhost:8000"]

# ── Email (SMTP) ──────────────────────────────────────────────────────────────
# Leave MAIL_USERNAME empty to use console logging in dev.
#
# Resend sandbox (no domain needed — emails only delivered to your Resend account email):
#   MAIL_SERVER=smtp.resend.com
#   MAIL_PORT=587
#   MAIL_USERNAME=resend
#   MAIL_PASSWORD=re_xxxxxxxxxxxxxxxxxxxx   ← your Resend API key
#   MAIL_FROM=onboarding@resend.dev         ← required for sandbox; use your domain once verified
#
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=noreply@wolkago.pl
MAIL_FROM_NAME=WolkaGo
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_TLS=true
MAIL_SSL=false

# ── Cloudflare R2 image storage ───────────────────────────────────────────────
# Leave all R2 vars empty in local dev — uploads fall back to local disk.
# In production, set all five in Render's environment dashboard.
#
# Steps to get these values:
#   1. cloudflare.com → R2 → Create bucket (e.g. "seller-site-uploads")
#   2. Enable "Public access" on the bucket to get the R2_PUBLIC_URL
#   3. R2 → Manage R2 API Tokens → Create Token → Object Read & Write on your bucket
#   4. Copy Account ID (shown on R2 overview page), Access Key ID, Secret Access Key
#
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=seller-site-uploads
R2_PUBLIC_URL=https://pub-xxxxxxxxxxxxxxxxxxxx.r2.dev

# ── Frontend URL (used in email verification links) ───────────────────────────
FRONTEND_BASE_URL=http://localhost:8000
